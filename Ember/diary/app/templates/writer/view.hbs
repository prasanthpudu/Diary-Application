{{page-title "View"}}
{{this.get}}
<Header @heading={{"viewer"}}/>
    <div class="option"></div>
    <div class="editer-container">
       {{#each this.data.notes as |text|}}
        <Note @id={{text.id}} @title={{text.title}} @text={{text.text}} @date={{text.time}} @stared={{text.stared}}/>
        
        {{/each}}
    </div>